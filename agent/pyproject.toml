[project]
name = "agenttether-agent"
version = "0.1.0"
description = "AI agent client for AgentTether — x402-paid blockchain event streams (LangGraph)"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["agenttether"]

[tool.pytest.ini_options]
testpaths = ["tests"]
